{
  "name": "jsx-alone-core",
  "version": "0.0.1",
  "description": "Write JSX without React or any other library, both in browser and server",
  "main": "dist/src/index.js",
  "types": "dist/src/index.d.ts",
  "typings": "dist/src/index.d.ts",
  "scripts": {
    "build": "npx tsc",
    "build-watch": "npx tsc -w",
    "clean": "rm -rf dist tmp",
    "test": "npx jest --testPathIgnorePatterns /dist/",
    "test-js": "npm run build && npx jest --testMatch \"**/dist/**/__tests__/**/*.js\"",
    "test-watch": "npm test -- --watch",
    "all": "npm run clean && npm run build && npm test",
    "prepare": "npm run all"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/cancerberoSgx/jsx-alone.git"
  },
  "keywords": [
    "SuiteCommerce",
    "TypeScript",
    "NetSuite",
    "devtools",
    "types",
    "JavaScript",
    "JSX",
    "TSX",
    "React"
  ],
  "author": "Sebastian Gurin",
  "license": "MIT",
  "homepage": "https://github.com/cancerberoSgx/jsx-alone",
  "dependencies": {},
  "devDependencies": {
    "@types/node": "^10.12.18",
    "jest": "^24.1.0",
    "@types/jest": "^24.0.6",
    "ts-jest": "^24.0.0",
    "typescript": "^3.2.2"
  }
}
