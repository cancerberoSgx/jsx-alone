module.exports = {
  launch: {
    dumpio: true,
    headless: false,
    args: ['--no-sandbox', '--disable-setuid-sandbox']
  },
  // server: {
  //   command: 'npm start',

// command: 'npm run build-dev && npx http-server ../docs/jsx-explorer '  
  // },
}